name: instagram_clone_fixed
description: "A new Flutter project."

publish_to: 'none'  # Keep this line for private packages.

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.1
  firebase_storage: ^12.4.0
  flutter_svg: ^2.0.17
  image_picker: ^1.1.2
  http: ^1.2.2
  provider: ^6.0.5
  uuid: ^4.5.1
  intl: ^0.20.1
  expandable_text: ^2.3.0
  zoom_pinch_overlay: ^1.3.2
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/  # Ensure this directory exists.
